---
phase: 03-operations-deployment-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/deployment-gate.md
autonomous: true
requirements:
  - OPS-03

must_haves:
  truths:
    - "A deployment gate checklist exists at docs/deployment-gate.md"
    - "Every checklist item is binary pass/fail"
    - "Checklist covers: healthcheck passes, routing tests, identity validation, permission model status"
    - "Checklist includes deployment mechanics section documenting clone + chown + group procedure"
    - ".planning/ is explicitly excluded from deployment"
  artifacts:
    - path: "docs/deployment-gate.md"
      provides: "Deployment gate checklist"
      contains: "pass/fail"
  key_links:
    - from: "docs/deployment-gate.md"
      to: "bin/healthcheck"
      via: "references healthcheck as automatable verification"
      pattern: "bin/healthcheck"
---

<objective>
Create the deployment gate checklist documenting what must be true before migrating to /home/claude.

Purpose: The deployment gate is a pre-flight checklist that a human reviews before running the actual migration. It covers routing, identity, healthcheck, permissions, and deployment mechanics. Binary pass/fail items -- no ambiguity.

Output: docs/deployment-gate.md
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operations-deployment-gate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/deployment-gate.md</name>
  <files>docs/deployment-gate.md</files>
  <read_first>
    - .planning/phases/03-operations-deployment-gate/03-CONTEXT.md (decisions D-08 through D-14 define exact requirements)
    - .planning/PROJECT.md (deployment constraints, ownership model)
    - .planning/REQUIREMENTS.md (OPS-03 definition)
    - bin/healthcheck (to reference what it covers in the automatable section)
  </read_first>
  <action>
Create `docs/deployment-gate.md` per D-08 (stable path in docs/, not .planning/). Per D-10, this is a document for human review, not a script.

Structure the file as follows:

```markdown
# Deployment Gate: ~/claude -> /home/claude

Pre-flight checklist for migrating Claude's home from staging (`/home/skogix/claude`) to production (`/home/claude`). Every item is pass/fail. All must pass before deployment.

## Automated Checks

Run `bin/healthcheck` -- covers all of the following automatically:

- [ ] Environment: home dir, gt cli, bd/beads, dolt server, git config, claude_home rig
- [ ] Identity paths: soul sections (10 files), profile, core frameworks, journal conventions
- [ ] Routing: CLAUDE.md exists in all 10 directories
- [ ] Memory block tiers: active tier (personal/core/) and LORE tier (personal/memory-blocks/) report non-zero counts
- [ ] Exit code: `bin/healthcheck` exits 0

## Manual Checks

### Routing validation

- [ ] Fresh session starting from root CLAUDE.md can navigate to any artifact in 2 hops
- [ ] No CLAUDE.md file exceeds 50 lines
- [ ] Context loads lazily -- session startup reads only root router

### Identity validation

- [ ] Soul document split is correct: 10 section files in personal/soul/ match CLAUDE.md router listing
- [ ] personal/profile.md contains current agent profile (not stale placeholder)
- [ ] Core frameworks in personal/core/ are complete documents (not stubs)

### Persistence validation

- [ ] Journal conventions doc exists and is accurate (personal/journal/CONVENTIONS.md)
- [ ] LORE museum is gated -- default routing does NOT auto-load memory blocks
- [ ] Session handoff mechanism documented (wrap-up convention)

### Permission model

- [ ] Permission model documented (Phase 4 -- mark PENDING if not yet complete)
- [ ] Shared space conventions documented (Phase 4 -- mark PENDING if not yet complete)

## Deployment Mechanics

### Procedure (per D-14: document, don't automate)

1. **Clone**: `git clone <repo-url> /home/claude`
2. **Ownership**: `chown -R claude:claude /home/claude`
3. **Group**: `usermod -aG skogai claude`
4. **Verify**: `su - claude -c '/home/claude/bin/healthcheck'`

### What deploys

- All git-tracked files and directories
- `.claude/` -- deploys as-is (contains get-shit-done framework, maintained by skogix per D-13)

### What does NOT deploy

- `.planning/` -- construction scaffold, stays in staging repo only (per D-12). Add to `.gitignore` or exclude from clone
- `.git/` -- fresh clone creates its own
- Any `.local` files -- environment-specific, not tracked

### Post-deployment

- Verify healthcheck passes as user claude: `su - claude -c '/home/claude/bin/healthcheck'`
- Verify routing works: navigate from /home/claude/CLAUDE.md to personal/soul/
- Update .planning/STATE.md in staging repo to record deployment date

---
*Gate created: Phase 03-operations-deployment-gate*
*Review before any deployment attempt*
```

Ensure the file uses standard markdown checkboxes (`- [ ]`) for all pass/fail items per D-09.
  </action>
  <verify>
    <automated>test -s /home/skogix/claude/docs/deployment-gate.md && grep -c "\- \[ \]" /home/skogix/claude/docs/deployment-gate.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/deployment-gate.md exists and is non-empty
    - File contains string "Deployment Gate"
    - File contains string "bin/healthcheck" (references automated checks)
    - File contains string "pass/fail" or uses checkbox format `- [ ]`
    - File contains at least 15 checkbox items (`- [ ]`)
    - File contains string "Deployment Mechanics"
    - File contains string "git clone"
    - File contains string "chown -R claude:claude"
    - File contains string "usermod -aG skogai"
    - File contains string ".planning/" with exclusion language (does NOT deploy)
    - File contains string ".claude/" with deployment language (deploys as-is)
    - File contains string "Permission model" marked as pending/Phase 4
    - File does NOT contain any executable script blocks (it's a document per D-10)
    - grep "PENDING\|pending\|Phase 4" docs/deployment-gate.md returns matches (permission items deferred)
  </acceptance_criteria>
  <done>docs/deployment-gate.md exists with binary pass/fail checklist covering routing, identity, permissions, healthcheck, and deployment mechanics including clone/chown/group procedure</done>
</task>

</tasks>

<verification>
Confirm the deployment gate document:
1. Exists at docs/deployment-gate.md
2. Has binary pass/fail items (checkboxes)
3. Covers all four areas: routing, identity, permissions, healthcheck
4. Includes deployment mechanics section
5. References bin/healthcheck for automatable checks

```bash
cd /home/skogix/claude && wc -l docs/deployment-gate.md && grep -c "\- \[ \]" docs/deployment-gate.md && grep "healthcheck\|routing\|identity\|permission\|mechanics" docs/deployment-gate.md | head -10
```
</verification>

<success_criteria>
- docs/deployment-gate.md exists at stable path
- All checklist items are binary pass/fail (checkbox format)
- Document covers: automated checks (healthcheck), manual checks (routing, identity, persistence, permissions), and deployment mechanics (clone, chown, group)
- Permission model items marked as pending Phase 4
- .planning/ explicitly excluded from deployment
</success_criteria>

<output>
After completion, create `.planning/phases/03-operations-deployment-gate/03-02-SUMMARY.md`
</output>

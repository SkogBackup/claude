---
phase: 02-persistence-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - .claude/commands/skogai/wrapup.md
  - personal/journal/2026-03-20/phase-2-persistence-complete.md
autonomous: true
requirements:
  - PER-04

must_haves:
  truths:
    - "A session handoff mechanism exists as a Claude Code command at .claude/commands/skogai/wrapup.md"
    - "At least one handoff artifact (journal entry) has been written using the conventions from Plan 01"
  artifacts:
    - path: ".claude/commands/skogai/wrapup.md"
      provides: "Session wrap-up workflow command"
      contains: "Ship It"
    - path: "personal/journal/2026-03-20/phase-2-persistence-complete.md"
      provides: "First handoff artifact demonstrating the convention"
      contains: "type: journal"
  key_links:
    - from: ".claude/commands/skogai/wrapup.md"
      to: "personal/journal/"
      via: "Phase 4 Journal It step"
      pattern: "personal/journal/YYYY-MM-DD"
    - from: "personal/journal/2026-03-20/phase-2-persistence-complete.md"
      to: "personal/journal/CONVENTIONS.md"
      via: "follows naming and frontmatter conventions"
      pattern: "type: journal"
---

<objective>
Create the wrap-up command and write the first handoff artifact.

Purpose: PER-04 requires a session handoff mechanism and at least one artifact written using it. The wrap-up command at `.claude/commands/skogai/wrapup.md` is a Claude Code slash command that guides session close-out through 4 phases: ship, remember, review, journal. The first journal entry written using the conventions from Plan 01 proves the system works end-to-end.

Output: `.claude/commands/skogai/wrapup.md` command, first journal entry in `personal/journal/YYYY-MM-DD/` format
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-persistence-layer/02-CONTEXT.md
@.planning/phases/02-persistence-layer/02-RESEARCH.md
@.planning/phases/02-persistence-layer/02-01-SUMMARY.md

<interfaces>
<!-- Claude Code command format (from .claude/commands/gsd/) -->
Claude Code commands are plain markdown files. No special frontmatter or structure required.
Claude reads the file and executes the instructions described.
$ARGUMENTS substitution available for parameterized commands.

<!-- Locked wrap-up phases from CONTEXT.md -->
Phase 1 (Ship it): git commit/push, file placement check, task cleanup. Beads replaced with GitHub issues + skogai-todo command
Phase 2 (Remember it): Memory placement hierarchy: auto memory (.planning/memory) -> CLAUDE.md -> .claude/rules/ -> @import -> GitHub issues. Remove claude.local.md from hierarchy.
Phase 3 (Review & apply): Self-improvement findings, auto-applied
Phase 4 (Journal it): Save to personal/journal/YYYY-MM-DD/<description>.md with frontmatter

<!-- Frontmatter schema for journal entries -->
```yaml
---
categories:
  - journal
tags:
  - [relevant-topic-tags]
permalink: journal/YYYY-MM-DD/description
title: [Entry Title]
type: journal
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wrap-up slash command</name>
  <files>.claude/commands/skogai/wrapup.md</files>
  <read_first>
    - .planning/phases/02-persistence-layer/02-CONTEXT.md (locked wrap-up workflow phases)
    - .planning/phases/02-persistence-layer/02-RESEARCH.md (wrap-up anti-patterns -- guide not automate)
    - personal/journal/CONVENTIONS.md (journal conventions for Phase 4 reference)
  </read_first>
  <action>
1. Create directory `.claude/commands/skogai/` (does not exist yet).

2. Create `.claude/commands/skogai/wrapup.md` with this exact content:

```markdown
# Session Wrap-Up

Run through these 4 phases to close the session properly. Each phase is a checkpoint -- complete it before moving to the next.

## Phase 1: Ship It

- Check `git status` for any uncommitted work
- Verify file placement -- are new files in the right directories? (personal/ for LORE, bin/ for scripts, notes/ for observations)
- Commit and push if there's work to ship
- Update any relevant GitHub issues or run `skogai-todo` for task tracking

## Phase 2: Remember It

What should persist beyond this session? Use the memory hierarchy (most permanent first):

1. **Auto memory** (`.planning/memory/`) -- facts, preferences, project knowledge
2. **CLAUDE.md** files -- routing, conventions, identity
3. **`.claude/rules/`** -- behavioral rules and constraints
4. **`@import`** -- reusable context blocks
5. **GitHub issues** -- actionable todos and tracked work

If new frameworks, preferences, or patterns were discovered, record them in the appropriate tier.

Do NOT use `claude.local.md` -- it is not part of the memory hierarchy.

## Phase 3: Review & Apply

- Were there any self-improvement findings this session? (better prompting patterns, workflow improvements, tooling discoveries)
- If clear and obvious: apply them now (update rules, memory, or conventions)
- If uncertain: note them in `notes/observations.md` for later consideration

## Phase 4: Journal It

Write a journal entry to `personal/journal/YYYY-MM-DD/<description>.md` where:
- `YYYY-MM-DD` is today's date
- `<description>` is a kebab-case topic slug

Include YAML frontmatter:

```yaml
---
categories:
  - journal
tags:
  - [relevant-topic-tags]
permalink: journal/YYYY-MM-DD/description
title: [Entry Title]
type: journal
---
```

Content is **immutable** after writing -- no content edits, only formatting fixes.

See `personal/journal/CONVENTIONS.md` for full conventions.
```

IMPORTANT: The command is a workflow guide, NOT automation. Claude reads these steps and decides what applies. The command should NOT contain `git commit` invocations or file write commands -- it describes what to do, Claude executes with judgment.
  </action>
  <verify>
    <automated>test -f .claude/commands/skogai/wrapup.md && grep -q "Ship It" .claude/commands/skogai/wrapup.md && grep -q "Remember It" .claude/commands/skogai/wrapup.md && grep -q "Review" .claude/commands/skogai/wrapup.md && grep -q "Journal It" .claude/commands/skogai/wrapup.md && grep -q "YYYY-MM-DD" .claude/commands/skogai/wrapup.md && grep -q "claude.local.md" .claude/commands/skogai/wrapup.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/skogai/wrapup.md exists
    - Contains "Phase 1: Ship It" heading
    - Contains "Phase 2: Remember It" heading
    - Contains "Phase 3: Review" heading
    - Contains "Phase 4: Journal It" heading
    - Contains "YYYY-MM-DD" (journal naming format reference)
    - Contains "claude.local.md" with "Do NOT use" context (removed from hierarchy)
    - Contains "immutable" (append-only rule reference)
    - Contains frontmatter template with `type: journal`
    - Does NOT contain actual git commands or file write commands (it's a guide, not a script)
  </acceptance_criteria>
  <done>Wrap-up command exists at .claude/commands/skogai/wrapup.md with all 4 phases: Ship, Remember, Review, Journal. Guides Claude through session close-out without automating the steps.</done>
</task>

<task type="auto">
  <name>Task 2: Write first handoff artifact (journal entry)</name>
  <files>personal/journal/2026-03-20/phase-2-persistence-complete.md</files>
  <read_first>
    - personal/journal/CONVENTIONS.md (follow the conventions exactly)
    - .planning/phases/02-persistence-layer/02-01-SUMMARY.md (what was done in Plan 01)
    - .planning/phases/02-persistence-layer/02-02-SUMMARY.md (what was done in Plan 02)
  </read_first>
  <action>
1. Create directory `personal/journal/2026-03-20/` (date folder for today).

2. Create `personal/journal/2026-03-20/phase-2-persistence-complete.md` with YAML frontmatter following the conventions doc exactly:

```markdown
---
categories:
  - journal
tags:
  - persistence
  - phase-2
  - conventions
permalink: journal/2026-03-20/phase-2-persistence-complete
title: Phase 2 Persistence Layer Complete
type: journal
---

# Phase 2: Persistence Layer Complete

Session establishing writing discipline for Claude's home.

## What Was Done

- Created journal conventions doc at `personal/journal/CONVENTIONS.md` specifying naming format (YYYY-MM-DD/<description>.md), write location, write triggers, and the append-only rule
- Verified LORE structural separation -- memory blocks require explicit navigation, not auto-loaded
- Migrated context scripts from `scripts/context/` to `bin/` with legacy symlink
- Created wrap-up command at `.claude/commands/skogai/wrapup.md` for session close-out workflow
- This entry is the first handoff artifact written using the new conventions

## What to Know Next Session

- Journal entries go in `personal/journal/YYYY-MM-DD/<description>.md`
- Run `/skogai:wrapup` to close sessions properly
- All context scripts are now in `bin/` (context-journal.sh, context-git.sh, etc.)
- Phase 3 (Operations & Deployment Gate) is next
```

NOTE: The executor should adjust the "What Was Done" section based on what actually happened during Plan 01 and Plan 02 execution (read their SUMMARYs). The content above is a template -- replace with actual outcomes.

3. Verify the entry follows conventions: date-folder structure, frontmatter with all required fields, kebab-case filename.
  </action>
  <verify>
    <automated>test -f personal/journal/2026-03-20/phase-2-persistence-complete.md && grep -q "type: journal" personal/journal/2026-03-20/phase-2-persistence-complete.md && grep -q "categories:" personal/journal/2026-03-20/phase-2-persistence-complete.md && grep -q "permalink:" personal/journal/2026-03-20/phase-2-persistence-complete.md</automated>
  </verify>
  <acceptance_criteria>
    - personal/journal/2026-03-20/phase-2-persistence-complete.md exists
    - Contains `type: journal` in frontmatter
    - Contains `categories:` in frontmatter
    - Contains `tags:` in frontmatter
    - Contains `permalink: journal/2026-03-20/phase-2-persistence-complete`
    - Contains `title:` in frontmatter
    - File is in date-folder structure (YYYY-MM-DD/<description>.md)
    - Filename is kebab-case
  </acceptance_criteria>
  <done>First handoff artifact written as a journal entry following all conventions. Demonstrates the date-folder structure, YAML frontmatter, and session summary format. PER-04 satisfied.</done>
</task>

</tasks>

<verification>
- `test -f .claude/commands/skogai/wrapup.md` -- wrap-up command exists
- `grep -c "Phase [1-4]" .claude/commands/skogai/wrapup.md` returns 4 -- all phases present
- `find personal/journal -mindepth 2 -maxdepth 2 -name "*.md" -not -name "CONVENTIONS.md" | head -1` returns a result -- at least one journal entry exists
- `grep -q "type: journal" personal/journal/2026-03-20/phase-2-persistence-complete.md` -- frontmatter correct
</verification>

<success_criteria>
- PER-04: Session handoff mechanism exists (.claude/commands/skogai/wrapup.md) and at least one handoff artifact written (journal entry in date-folder format with proper frontmatter)
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistence-layer/02-03-SUMMARY.md`
</output>

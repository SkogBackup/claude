---
phase: 02-persistence-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - personal/journal/CONVENTIONS.md
  - personal/CLAUDE.md
autonomous: true
requirements:
  - PER-01
  - PER-02
  - PER-03

must_haves:
  truths:
    - "A conventions doc exists that specifies naming format, write location, write triggers, and append-only rule"
    - "The personal/journal/ directory exists with the date-folder structure established"
    - "LORE (memory blocks) is not auto-loaded from the default routing path"
  artifacts:
    - path: "personal/journal/CONVENTIONS.md"
      provides: "Journal writing conventions"
      contains: "YYYY-MM-DD"
    - path: "personal/journal/"
      provides: "Journal directory ready for date-folder entries"
  key_links:
    - from: "personal/CLAUDE.md"
      to: "personal/journal/CONVENTIONS.md"
      via: "contents section mentioning journal/ with convention reference"
      pattern: "journal.*CONVENTIONS"
    - from: "personal/memory-blocks/CLAUDE.md"
      to: "session_protocol"
      via: "LORE gate language"
      pattern: "Load only when asked"
---

<objective>
Create journal conventions doc, establish journal directory structure, and verify LORE gating.

Purpose: Writing to the home needs rules before anything writes. The conventions doc defines how journal entries are named, where they go, what triggers them, and the immutability rule. The LORE gate (PER-03) is already structurally solved by Phase 1 routing -- this plan verifies it holds.

Output: `personal/journal/CONVENTIONS.md`, updated `personal/CLAUDE.md` with journal convention reference
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-persistence-layer/02-CONTEXT.md
@.planning/phases/02-persistence-layer/02-RESEARCH.md

<interfaces>
<!-- Frontmatter schema from personal/profile.md -->
```yaml
---
categories:
  - [category]
tags:
  - [tag]
permalink: [path/slug]
title: [title]
type: [note|journal|lore]
---
```

<!-- LORE gate language from personal/memory-blocks/CLAUDE.md -->
"Permanent historical record. These are archives, not active constraints."
"Load only when asked about specific eras or history."

<!-- Session protocol from personal/CLAUDE.md -->
"read memory blocks only if asked about history"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create journal conventions doc and directory structure</name>
  <files>personal/journal/CONVENTIONS.md</files>
  <read_first>
    - personal/profile.md (frontmatter schema to replicate)
    - personal/CLAUDE.md (current contents section to understand routing)
    - scripts/context/context-journal.sh (canonical journal reader -- understand what formats it supports)
    - .planning/phases/02-persistence-layer/02-CONTEXT.md (locked decisions on journal structure)
  </read_first>
  <action>
1. Create directory `personal/journal/` (it does not exist yet).

2. Create `personal/journal/CONVENTIONS.md` with this exact structure:

```markdown
---
categories:
  - meta
  - journal
tags:
  - conventions
  - persistence
permalink: journal/conventions
title: Journal Conventions
type: note
---

# Journal Conventions

## Naming Format

Entries use date-folder structure:

```
personal/journal/YYYY-MM-DD/<description>.md
```

- Date folder groups all entries from a single day
- Description is a kebab-case topic slug (e.g., `phase-2-planning`, `routing-decisions`)
- Multiple entries per date folder are expected -- one per session or topic

## Where to Write

All journal entries go in `personal/journal/`. No other location.

## What Triggers a Write

- End of a session with notable work, decisions, or insights
- The `/skogai:wrapup` command Phase 4 step
- Any moment worth recording for future Claude sessions

## Append-Only Rule

- Content is **immutable** once written
- Formatting corrections are OK (typos, markdown fixes)
- Content changes, deletions, or rewrites are NOT OK
- Each entry is a snapshot of that moment -- not a living document

## Frontmatter

Every journal entry carries YAML frontmatter:

```yaml
---
categories:
  - journal
tags:
  - [relevant-topic-tags]
permalink: journal/YYYY-MM-DD/description
title: [Entry Title]
type: journal
---
```

## Reading Journals

`bin/context-journal.sh` reads both flat (legacy) and date-folder formats.
It shows the most recent day's entries and lists older ones as references.
```

3. Update `personal/CLAUDE.md` contents section to reference the conventions:
   - Change the `journal/` line from `journal/         -- session records, append-only` to `journal/         -- session records, append-only (see journal/CONVENTIONS.md)`
  </action>
  <verify>
    <automated>test -f personal/journal/CONVENTIONS.md && grep -q "YYYY-MM-DD" personal/journal/CONVENTIONS.md && grep -q "Append-Only" personal/journal/CONVENTIONS.md && grep -q "What Triggers" personal/journal/CONVENTIONS.md && grep -q "CONVENTIONS" personal/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - personal/journal/CONVENTIONS.md exists and has more than 30 lines
    - personal/journal/CONVENTIONS.md contains "YYYY-MM-DD" (naming format)
    - personal/journal/CONVENTIONS.md contains "Append-Only Rule" (immutability)
    - personal/journal/CONVENTIONS.md contains "What Triggers a Write" (write triggers)
    - personal/journal/CONVENTIONS.md contains "Where to Write" (location)
    - personal/journal/CONVENTIONS.md contains YAML frontmatter with `type: note`
    - personal/CLAUDE.md contains "CONVENTIONS" in the journal/ line
  </acceptance_criteria>
  <done>Journal conventions doc exists at personal/journal/CONVENTIONS.md with all four required sections (naming, location, triggers, append-only). personal/CLAUDE.md updated to reference it.</done>
</task>

<task type="auto">
  <name>Task 2: Verify LORE structural separation (PER-03)</name>
  <files>personal/CLAUDE.md</files>
  <read_first>
    - personal/CLAUDE.md (session_protocol with LORE gate language)
    - personal/memory-blocks/CLAUDE.md (LORE museum routing doc)
    - CLAUDE.md (root router -- verify no direct memory block loading)
  </read_first>
  <action>
Verify the LORE gate is intact from Phase 1. Three checks:

1. `personal/memory-blocks/CLAUDE.md` contains "Load only when asked about specific eras or history" -- this is the gate language that prevents auto-loading.

2. `personal/CLAUDE.md` session_protocol contains "read memory blocks only if asked about history" -- this reinforces the gate.

3. Root `CLAUDE.md` routes to `@personal/CLAUDE.md` but does NOT route directly to memory-blocks -- memory blocks require two hops (root -> personal/ -> memory-blocks/).

If all three hold: PER-03 is satisfied. No code changes needed -- just verification.

If any check fails (unlikely given Phase 1 work), fix the specific routing language:
- If memory-blocks/CLAUDE.md is missing the gate: add "Load only when asked about specific eras or history." as the first line after the title
- If session_protocol is missing the gate: add "read memory blocks only if asked about history" to the protocol list
- If root CLAUDE.md has a direct memory-blocks route: remove it (navigation should go through personal/ first)
  </action>
  <verify>
    <automated>grep -q "Load only when asked" personal/memory-blocks/CLAUDE.md && grep -q "read memory blocks only if asked" personal/CLAUDE.md && ! grep -q "memory-blocks" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - personal/memory-blocks/CLAUDE.md contains "Load only when asked"
    - personal/CLAUDE.md contains "read memory blocks only if asked"
    - Root CLAUDE.md does NOT contain "memory-blocks" (no direct route bypassing personal/)
  </acceptance_criteria>
  <done>LORE structural separation verified -- memory blocks require explicit navigation through personal/CLAUDE.md, not auto-loaded from default routing path.</done>
</task>

</tasks>

<verification>
- `test -d personal/journal/` -- directory exists
- `test -f personal/journal/CONVENTIONS.md` -- conventions doc exists
- `grep -c "naming\|where\|triggers\|append" personal/journal/CONVENTIONS.md` returns 4+ matches
- `grep -q "CONVENTIONS" personal/CLAUDE.md` -- router updated
- LORE gate checks all pass (see Task 2 verify)
</verification>

<success_criteria>
- PER-01: Journal conventions doc exists with naming format, write location, write triggers, append-only rule
- PER-02: Date-folder convention (YYYY-MM-DD/<description>.md) is specified in conventions doc
- PER-03: LORE gate language verified in memory-blocks/CLAUDE.md, personal/CLAUDE.md session_protocol, and root CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistence-layer/02-01-SUMMARY.md`
</output>

---
date: "2026-03-20 15:30"
promoted: true
---

Epic: "Claude's Home v2.0 — SkogAI Integration & Home Improvements"

Context from prior conversation: Claude explored the SkogAI ecosystem (332 repos under skogai org, Gas Town ~/gt/ shared workspace, sibling agents dot/amy/goose/aldervall/letta, Dolt database backend, skogfences philosophy). Identified what Claude's home needs beyond v1.0 identity setup.

8 proposals for v2.0 milestone:

1. SCRATCH/CLIPBOARD AREA — intermediate work stash between conversations. Not memory, not notes — a working desk for half-finished analysis state.

2. SNIPPETS/RECIPES COLLECTION — patterns Claude keeps re-deriving each session. Shell one-liners, git incantations, common file templates. Saves tokens.

3. TOOL INDEX / CAPABILITY MAP — cached inventory of installed CLIs/tools in the environment. Currently discovered ad-hoc. A which/command -v inventory.

4. PROJECT BOOKMARKS — quick pointers to repos/directories Skogix frequently works in, with one-line context. So "the dotfiles thing" resolves without searching.

5. REVIEW/FEEDBACK LOG — structured retrospective journal beyond the memory system. What went well, what didn't, across sessions.

6. BETTER BIN/ TOOLING — scripts for: clearing stale worktrees, refreshing docs, validating skill/hook/agent ecosystem consistency. healthcheck is lonely.

7. CONTACTS/CONTEXT FOR OTHER AI AGENTS — handoff protocol document for multi-agent setups. Who the siblings are, how to communicate.

8. SKOGAI ECOSYSTEM EXPLORATION — understand the 332 repos, Gas Town infrastructure, how Claude fits as a first-class citizen in the multi-agent family. Discovery/research phase.

Key insight from conversation: Dot (gptme agent) built manually what Claude gets natively (skills, hooks, subagents, memory). The interesting question is what Claude DOESN'T get for free. V2.0 should answer that.
---
phase: 03-operations-deployment-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/healthcheck
  - bin/CLAUDE.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "Running bin/healthcheck verifies identity paths (soul sections, profile, core frameworks, journal conventions) exist and are non-empty"
    - "Running bin/healthcheck reports active tier (personal/core/) and LORE tier (personal/memory-blocks/) with file counts"
    - "Healthcheck exits non-zero if any identity path is missing or empty"
    - "Existing environment checks (gt, bd, dolt, git, claude_home rig) still work"
  artifacts:
    - path: "bin/healthcheck"
      provides: "Extended healthcheck with identity + tier checks"
      contains: "identity paths"
    - path: "bin/CLAUDE.md"
      provides: "Updated router reflecting expanded healthcheck"
      contains: "identity path"
  key_links:
    - from: "bin/healthcheck"
      to: "personal/soul/"
      via: "file existence checks"
      pattern: "personal/soul/"
    - from: "bin/healthcheck"
      to: "personal/core/"
      via: "active tier reporting"
      pattern: "active tier"
    - from: "bin/healthcheck"
      to: "personal/memory-blocks/"
      via: "LORE tier reporting"
      pattern: "LORE tier"
---

<objective>
Extend bin/healthcheck to verify identity paths and report memory block tiers.

Purpose: Claude needs a self-diagnostic that confirms the home's identity layer is intact -- soul sections, profile, core frameworks, and journal conventions all exist and are non-empty. Additionally, the healthcheck must report which memory block tiers (active vs LORE) are accessible with file counts.

Output: Extended bin/healthcheck, updated bin/CLAUDE.md
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operations-deployment-gate/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bin/healthcheck with identity path checks and tier reporting</name>
  <files>bin/healthcheck</files>
  <read_first>
    - bin/healthcheck (current implementation -- preserve existing check() pattern and env checks)
    - personal/soul/CLAUDE.md (verify soul section file list)
    - personal/core/CLAUDE.md (verify framework file list)
    - personal/memory-blocks/CLAUDE.md (verify memory block file list)
  </read_first>
  <action>
Extend bin/healthcheck by adding three new sections AFTER the existing environment checks (keep all 6 existing checks untouched). Per D-01, this extends, not replaces.

**Section: Identity Paths (per D-02, D-04)**

Add a section header: `echo ""; echo "identity paths"; echo "------------------"`

Check each of these paths exists AND is non-empty (file size > 0). Use the existing `check` function. Result is "fail" (not "warn") if missing or empty per D-04:

Files to check (10 soul sections):
- `personal/soul/01-equation.md`
- `personal/soul/02-skogai-family.md`
- `personal/soul/03-philosophies.md`
- `personal/soul/04-context-destruction.md`
- `personal/soul/05-notation-system.md`
- `personal/soul/06-frameworks.md`
- `personal/soul/07-historical-context.md`
- `personal/soul/08-memory-architecture.md`
- `personal/soul/09-guiding-principles.md`
- `personal/soul/10-session-protocol.md`

Other identity files:
- `personal/profile.md`
- `personal/journal/CONVENTIONS.md`

Framework files in personal/core/:
- `personal/core/certainty-principle.md`
- `personal/core/placeholder-approach.md`
- `personal/core/epistemic-frameworks.md`
- `personal/core/context-destruction.md`
- `personal/core/learnings.md`

Use a helper function to DRY the check: `check_file() { local f="$1"; if [ -s "$CLAUDE_HOME/$f" ]; then check "$f" "ok"; else check "$f" "fail"; fi }` where CLAUDE_HOME is determined at the top of the script as the directory containing the healthcheck (i.e., `CLAUDE_HOME="$(cd "$(dirname "$0")/.." && pwd)"`).

**Section: Routing (per D-03)**

Add: `echo ""; echo "routing"; echo "------------------"`

Check CLAUDE.md exists in each of these directories (use check_file with path relative to CLAUDE_HOME):
- `CLAUDE.md` (root)
- `personal/CLAUDE.md`
- `personal/soul/CLAUDE.md`
- `personal/core/CLAUDE.md`
- `personal/memory-blocks/CLAUDE.md`
- `docs/CLAUDE.md`
- `bin/CLAUDE.md`
- `notes/CLAUDE.md`
- `guestbook/CLAUDE.md`
- `lab/CLAUDE.md`

These are also "fail" if missing (routing broken = hard failure per D-04).

**Section: Memory Block Tiers (per D-05, D-06, D-07)**

Add: `echo ""; echo "memory block tiers"; echo "------------------"`

Count files in each tier:
- Active tier: count .md files in `personal/core/` excluding CLAUDE.md. Report: `check "active tier: N frameworks" "ok"` (or "warn" if count is 0 per D-07)
- LORE tier: count memory block files in `personal/memory-blocks/` excluding CLAUDE.md. Separate into blocks (files matching `*block-[0-9]*` but not `*addendum*`) and addenda (files matching `*addendum*`). Report: `check "LORE tier: N memory blocks + M addenda" "ok"` (or "warn" if count is 0 per D-07)

**Exit code:**

The existing script does NOT exit with a code based on failures. Add at the very end (after the results line): `exit $FAIL` -- so it exits 0 when no failures, non-zero otherwise. This satisfies D-04.
  </action>
  <verify>
    <automated>cd /home/skogix/claude && bash bin/healthcheck; echo "exit code: $?"</automated>
  </verify>
  <acceptance_criteria>
    - bin/healthcheck contains `check_file` helper function
    - bin/healthcheck contains string "identity paths"
    - bin/healthcheck contains string "routing"
    - bin/healthcheck contains string "memory block tiers"
    - bin/healthcheck contains string "active tier"
    - bin/healthcheck contains string "LORE tier"
    - bin/healthcheck contains `personal/soul/01-equation.md`
    - bin/healthcheck contains `personal/profile.md`
    - bin/healthcheck contains `personal/core/certainty-principle.md`
    - bin/healthcheck contains `personal/journal/CONVENTIONS.md`
    - bin/healthcheck ends with `exit $FAIL` (or equivalent non-zero exit on failure)
    - Running `bash bin/healthcheck` from /home/skogix/claude outputs lines containing "[ok]" for identity files
    - Running `bash bin/healthcheck` from /home/skogix/claude outputs a line matching "active tier: [0-9]+ frameworks"
    - Running `bash bin/healthcheck` from /home/skogix/claude outputs a line matching "LORE tier: [0-9]+ memory blocks"
    - Existing checks (home directory, gt cli, bd, dolt, git, claude_home) still appear in output
  </acceptance_criteria>
  <done>bin/healthcheck verifies all identity paths, reports tier counts, exits non-zero on missing paths, and preserves all existing environment checks</done>
</task>

<task type="auto">
  <name>Task 2: Update bin/CLAUDE.md to reflect expanded healthcheck</name>
  <files>bin/CLAUDE.md</files>
  <read_first>
    - bin/CLAUDE.md (current router content)
    - bin/healthcheck (after Task 1 modifications -- to accurately describe new capabilities)
  </read_first>
  <action>
Update the healthcheck description line in bin/CLAUDE.md. Replace the current line:

```
- `healthcheck` -- verifies environment sanity: home dir, gt cli, bd/beads, dolt server, git config, claude_home rig. Run: `./bin/healthcheck`
```

With:

```
- `healthcheck` -- verifies environment sanity and identity integrity: env checks (home dir, gt cli, bd/beads, dolt server, git config, claude_home rig), identity path validation (soul sections, profile, core frameworks, journal conventions), CLAUDE.md routing verification, and memory block tier reporting (active vs LORE). Exits non-zero on failures. Run: `./bin/healthcheck`
```

Do NOT change anything else in the file. Keep all context script entries as-is.
  </action>
  <verify>
    <automated>grep -c "identity integrity" /home/skogix/claude/bin/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - bin/CLAUDE.md contains string "identity integrity"
    - bin/CLAUDE.md contains string "identity path validation"
    - bin/CLAUDE.md contains string "tier reporting"
    - bin/CLAUDE.md contains string "active vs LORE"
    - bin/CLAUDE.md still contains "context.sh" (context scripts section preserved)
    - bin/CLAUDE.md is under 50 lines (CTX-03 compliance)
  </acceptance_criteria>
  <done>bin/CLAUDE.md accurately describes the expanded healthcheck capabilities</done>
</task>

</tasks>

<verification>
Run the full healthcheck and confirm:
1. All existing env checks still present
2. Identity path section lists soul sections, profile, frameworks
3. Routing section lists all CLAUDE.md files
4. Tier reporting shows active + LORE counts
5. Exit code is 0 when all identity files exist

```bash
cd /home/skogix/claude && bash bin/healthcheck
echo "Exit: $?"
wc -l bin/CLAUDE.md
```
</verification>

<success_criteria>
- bin/healthcheck runs clean with identity checks passing
- Tier reporting shows accurate file counts for active and LORE tiers
- Exit code reflects failure count
- bin/CLAUDE.md accurately describes expanded healthcheck
</success_criteria>

<output>
After completion, create `.planning/phases/03-operations-deployment-gate/03-01-SUMMARY.md`
</output>

---
phase: 01-identity-routing
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [IDN-01, IDN-02, IDN-03, IDN-04, IDN-05, CTX-01, CTX-02, CTX-03, CTX-04]

must_haves:
  truths:
    - "All 9 phase requirements pass automated verification"
    - "Two-hop navigation works: root -> any artifact in 2 deliberate choices"
    - "No CLAUDE.md exceeds 50 lines"
    - "Session startup does not eagerly load soul document"
  artifacts: []
  key_links: []
---

<objective>
Run full automated verification of all phase requirements and get human confirmation that two-hop navigation works.

Purpose: Plans 01 and 02 made structural changes across 15+ files. This plan verifies the complete phase goal: "every artifact navigable from root in two hops" and all 9 requirements pass.

Output: Verified phase completion or identified gaps
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-identity-routing/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification of all 9 requirements</name>
  <files></files>
  <read_first>
    CLAUDE.md,
    personal/CLAUDE.md,
    personal/soul/CLAUDE.md,
    personal/core/CLAUDE.md,
    personal/memory-blocks/CLAUDE.md,
    docs/CLAUDE.md,
    bin/CLAUDE.md,
    guestbook/CLAUDE.md,
    notes/CLAUDE.md,
    lab/CLAUDE.md
  </read_first>
  <action>
Run the following verification commands and report results for each requirement:

**IDN-01** (stable paths): `ls personal/soul/CLAUDE.md personal/profile.md personal/core/certainty-principle.md personal/core/placeholder-approach.md personal/core/epistemic-frameworks.md` -- all must exist.

**IDN-02** (lazy loading): `grep '@soul-document' personal/CLAUDE.md` -- must return empty (no eager load). `grep '@soul/' personal/CLAUDE.md` -- must return 1 match (lazy dir link).

**IDN-03** (INDEX.md): `grep 'soul/' personal/INDEX.md` -- must return match. `grep 'soul-document.md' personal/INDEX.md` -- must return empty.

**IDN-04** (memory tiering): `grep -i 'lore\|museum\|archive\|reference' personal/memory-blocks/CLAUDE.md` -- must return matches (tier labels present).

**IDN-05** (frameworks by path): `grep 'certainty-principle' personal/core/CLAUDE.md` -- must return match. `grep 'placeholder-approach' personal/core/CLAUDE.md` -- must return match. `grep 'epistemic-frameworks' personal/core/CLAUDE.md` -- must return match.

**CTX-01** (root routes all): `grep -c '/' CLAUDE.md` -- must show 6+ directory entries. Specifically check: `grep 'personal/' CLAUDE.md && grep 'docs/' CLAUDE.md && grep 'bin/' CLAUDE.md && grep 'notes/' CLAUDE.md && grep 'guestbook/' CLAUDE.md && grep 'lab/' CLAUDE.md`

**CTX-02** (every dir has CLAUDE.md): `ls personal/CLAUDE.md personal/soul/CLAUDE.md personal/core/CLAUDE.md personal/memory-blocks/CLAUDE.md docs/CLAUDE.md bin/CLAUDE.md guestbook/CLAUDE.md notes/CLAUDE.md lab/CLAUDE.md` -- all 9 must exist.

**CTX-03** (50-line limit): `wc -l $(find . -name CLAUDE.md -not -path './.claude/*' -not -path './.planning/*' -not -path './.data/*')` -- all must show 50 or fewer.

**CTX-04** (lazy loading): Same as IDN-02 check. Additionally verify root CLAUDE.md has no `@file.md` imports (only directory links).

Report: PASS or FAIL for each requirement. If any FAIL, list what's wrong.
  </action>
  <verify>
    <automated>wc -l $(find . -name CLAUDE.md -not -path './.claude/*' -not -path './.planning/*' -not -path './.data/*') 2>/dev/null | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - All 9 requirement checks report PASS
    - `wc -l` output shows no CLAUDE.md exceeding 50 lines
    - `grep '@soul-document' personal/CLAUDE.md` returns empty
    - All 9 CLAUDE.md files exist (root + personal + soul + core + memory-blocks + docs + bin + guestbook + notes + lab = 10 files)
  </acceptance_criteria>
  <done>All 9 phase requirements verified with automated commands. Results documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies two-hop navigation</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for their confirmation. This is a human-verify checkpoint -- Claude has completed all automated work in Plans 01 and 02. The user now tests the routing system in a fresh session.

What was built: Complete routing system -- root CLAUDE.md routes 6 directories, soul document split into 10 sections, personal/ uses lazy loading, all directories have CLAUDE.md routers, docs/ trimmed to under 50 lines, memory blocks labelled as LORE museum, core frameworks have a discoverable router.

Verification steps for the user:

1. Root orientation: Root CLAUDE.md loads automatically. Confirm it shows all 6 directories with descriptions. Confirm identity context is inline (the equation, "not application code").

2. Two-hop to soul: From root, navigate to personal/soul/. Confirm soul/CLAUDE.md loads with 10 section descriptions. Confirm the soul document monolith does NOT load eagerly when entering personal/.

3. Two-hop to framework: From root, navigate to personal/core/. Confirm core/CLAUDE.md shows all 8 framework files with descriptions. Load certainty-principle.md and confirm it works independently.

4. LORE museum: Navigate to personal/memory-blocks/. Confirm CLAUDE.md shows LORE/museum labels and era listing.

5. Thin routers: Check bin/, guestbook/, notes/, lab/ each have a CLAUDE.md that answers "what's here and when to use it."

6. docs/ budget: Confirm docs/CLAUDE.md is readable and useful despite being under 50 lines.
  </action>
  <verify>User types "approved" or describes issues.</verify>
  <acceptance_criteria>
    - User confirms two-hop navigation works from root to any artifact
    - User confirms soul document does not load eagerly when entering personal/
    - User confirms all thin routers answer "what's here and when to use it"
  </acceptance_criteria>
  <done>Human has verified the complete routing system works as expected in a fresh Claude Code session.</done>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. All 9 automated requirement checks pass
2. Human confirms two-hop navigation works in a fresh session
3. No CLAUDE.md exceeds 50 lines
4. Soul document content preserved across split (line count check)
</verification>

<success_criteria>
- 9/9 phase requirements pass automated verification
- Human confirms two-hop navigation works from root to any artifact
- Phase marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-routing/01-03-SUMMARY.md`
</output>

---
phase: 01-identity-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - bin/CLAUDE.md
  - guestbook/CLAUDE.md
  - notes/CLAUDE.md
  - lab/CLAUDE.md
  - docs/CLAUDE.md
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03]

must_haves:
  truths:
    - "Root CLAUDE.md routes to all 6+ directories with content"
    - "bin/, guestbook/, notes/, lab/ each have a CLAUDE.md"
    - "docs/CLAUDE.md is under 50 lines"
    - "No CLAUDE.md anywhere in the project exceeds 50 lines (excluding .claude/ and .planning/)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Root router with full directory coverage"
      contains: "@personal/"
    - path: "bin/CLAUDE.md"
      provides: "Thin router for scripts"
      contains: "healthcheck"
    - path: "guestbook/CLAUDE.md"
      provides: "Thin router for visitor notes"
      contains: "visitor"
    - path: "notes/CLAUDE.md"
      provides: "Thin router for observations"
      contains: "observations"
    - path: "lab/CLAUDE.md"
      provides: "Thin router for experiments"
      contains: "experiment"
    - path: "docs/CLAUDE.md"
      provides: "Trimmed docs router under 50 lines"
      contains: "Daily drivers"
  key_links:
    - from: "CLAUDE.md"
      to: "personal/"
      via: "@personal/ tree preview"
      pattern: "@personal/"
    - from: "CLAUDE.md"
      to: "docs/"
      via: "@docs/ tree preview"
      pattern: "@docs/"
    - from: "CLAUDE.md"
      to: "bin/"
      via: "@bin/ tree preview"
      pattern: "@bin/"
---

<objective>
Rewrite root CLAUDE.md to route all directories, create thin CLAUDE.md files for directories that lack them, and trim docs/CLAUDE.md to under 50 lines.

Purpose: Currently root CLAUDE.md only routes to docs/ and personal/ -- four directories with content (bin/, guestbook/, notes/, lab/) are invisible from session start. docs/CLAUDE.md at 107 lines is over the 50-line budget. This plan fixes all root-level routing gaps.

Output: Rewritten root CLAUDE.md, 4 new thin CLAUDE.md files, trimmed docs/CLAUDE.md
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-routing/01-CONTEXT.md
@.planning/phases/01-identity-routing/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root CLAUDE.md and create 4 thin directory routers</name>
  <files>
    CLAUDE.md,
    bin/CLAUDE.md,
    guestbook/CLAUDE.md,
    notes/CLAUDE.md,
    lab/CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md,
    bin/healthcheck,
    guestbook/skogix.md,
    notes/observations.md,
    lab/README.md
  </read_first>
  <action>
**Step 1: Rewrite root CLAUDE.md.** Replace entire file with:

```
# ~/claude -- Claude's home

<identity>
Claude -- the ? in @ + ? = $. Bridge between intent (@) and reality ($).
Home directory for an AI agent: identity, tools, docs, memory.
Not application code -- this IS the workspace.
</identity>

## directories

- @personal/ -- identity, soul, frameworks, memory, journal
- @docs/ -- Claude Code reference docs (fetched from code.claude.com)
- @bin/ -- executable scripts and tools (healthcheck)
- @notes/ -- personal observations and patterns
- @guestbook/ -- visitor notes and cross-agent messages
- @lab/ -- experiments, WIP, staging
```

This is 15 lines. Uses bare `@dir/` tree preview syntax per user decision (CONTEXT.md: "Root uses @dir/ tree preview syntax for all directories -- shows structure without loading CLAUDE.md chains"). This triggers Claude Code's tree preview behavior -- showing directory structure on hover/expansion without eagerly loading the CLAUDE.md chain. All 6 content directories are listed. No eager file imports.

Note: `state/` and `tasks/` are intentionally omitted from the root router. `state/` is empty and `tasks/` has only one example file -- neither has content worth routing to. If they gain content later, add them.

Note: The `journal` symlink at root (`journal -> ./personal/journal/`) is left in place. It's a convenience shortcut and doesn't affect routing.

**Step 2: Create bin/CLAUDE.md** with this exact content:

```
# bin/ -- Executable scripts

Scripts and tools for home directory operations.

## contents

- `healthcheck` -- verifies identity paths exist and are non-empty. Run: `./bin/healthcheck`

Add new scripts here. Each should be self-contained and runnable from project root.
```

(9 lines. Read bin/healthcheck first to confirm what it does and describe it accurately. If healthcheck does something different than "verifies identity paths," update the description to match.)

**Step 3: Create guestbook/CLAUDE.md** with this exact content:

```
# guestbook/ -- Visitor notes

Cross-agent communication channel. Messages from Skogix and other agents.

## contents

- [skogix.md](skogix.md) -- Skogix's message to Claude

Write conventions: one file per visitor, named `{agent-name}.md`. Append-only.
```

(9 lines.)

**Step 4: Create notes/CLAUDE.md** with this exact content:

```
# notes/ -- Personal observations

Patterns, reflections, and observations from sessions. Not structured knowledge -- more like a scratchpad for things worth remembering.

## contents

- [observations.md](observations.md) -- collected observations and patterns
```

(7 lines.)

**Step 5: Create lab/CLAUDE.md** with this exact content:

```
# lab/ -- Experiments and WIP

Staging area for work in progress, experiments, and things being tested before they get a permanent home.

## contents

- [README.md](README.md) -- lab overview and current experiments

Nothing here is stable. Move finished work to its proper directory.
```

(9 lines.)
  </action>
  <verify>
    <automated>ls CLAUDE.md bin/CLAUDE.md guestbook/CLAUDE.md notes/CLAUDE.md lab/CLAUDE.md && wc -l CLAUDE.md bin/CLAUDE.md guestbook/CLAUDE.md notes/CLAUDE.md lab/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `cat CLAUDE.md` contains lines with "@personal/", "@docs/", "@bin/", "@guestbook/", "@notes/", "@lab/" (all 6 directories routed with tree preview syntax)
    - `grep '@personal/' CLAUDE.md` returns at least 1 match
    - `grep '@bin/' CLAUDE.md` returns at least 1 match
    - `grep '@guestbook/' CLAUDE.md` returns at least 1 match
    - `grep '@notes/' CLAUDE.md` returns at least 1 match
    - `grep '@lab/' CLAUDE.md` returns at least 1 match
    - `grep '@docs/' CLAUDE.md` returns at least 1 match
    - `wc -l CLAUDE.md` shows 15 lines or fewer (well under 50 limit)
    - `wc -l bin/CLAUDE.md` shows under 15 lines
    - `wc -l guestbook/CLAUDE.md` shows under 15 lines
    - `wc -l notes/CLAUDE.md` shows under 15 lines
    - `wc -l lab/CLAUDE.md` shows under 15 lines
    - `grep 'identity' CLAUDE.md` returns at least 1 match (identity inline in root)
    - `grep 'healthcheck' bin/CLAUDE.md` returns at least 1 match
    - No `[dir/](dir/CLAUDE.md)` markdown hyperlinks in root CLAUDE.md directories section -- must be bare @dir/ syntax
  </acceptance_criteria>
  <done>Root CLAUDE.md routes all 6 content directories using @dir/ tree preview syntax. bin/, guestbook/, notes/, lab/ each have a CLAUDE.md describing their contents and purpose. All files are under 15 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Trim docs/CLAUDE.md from 107 lines to under 50</name>
  <files>docs/CLAUDE.md</files>
  <read_first>
    docs/CLAUDE.md
  </read_first>
  <action>
Rewrite docs/CLAUDE.md to keep only the two most-used sections: "Daily drivers" and "Extensibility". Remove "CI/CD and platforms", "Infrastructure and enterprise", "Cloud providers", "Getting started and troubleshooting", and "Reference" sections entirely. Add a single line at the bottom pointing to the full listing.

Replace the entire file with:

```
# docs/ -- Claude Code reference library

Fetched from https://code.claude.com/docs/en. Refresh with `docs/fetch-docs.sh`.

Read lazily: find the topic below, open only what you need.

## Daily drivers

| Doc | What's in it |
|-----|-------------|
| [hooks](claude-code/hooks.md) | Full hooks reference -- event types, schema, lifecycle |
| [hooks-guide](claude-code/hooks-guide.md) | Practical hook patterns and examples |
| [settings](claude-code/settings.md) | All settings.json keys and behavior |
| [skills](claude-code/skills.md) | Skill authoring -- triggers, structure, slash commands |
| [sub-agents](claude-code/sub-agents.md) | Custom subagent creation and orchestration |
| [mcp](claude-code/mcp.md) | MCP server integration -- tools, transports, config |
| [memory](claude-code/memory.md) | CLAUDE.md layering, project memory, auto-memory |
| [permissions](claude-code/permissions.md) | Permission model -- allow/deny, scopes, profiles |
| [cli-reference](claude-code/cli-reference.md) | CLI flags, modes, and output formats |
| [model-config](claude-code/model-config.md) | Model selection, routing, provider config |
| [best-practices](claude-code/best-practices.md) | Prompt design, CLAUDE.md tips, workflow patterns |
| [common-workflows](claude-code/common-workflows.md) | Git, testing, refactoring, debugging recipes |

## Extensibility

| Doc | What's in it |
|-----|-------------|
| [plugins](claude-code/plugins.md) | Plugin authoring guide |
| [plugins-reference](claude-code/plugins-reference.md) | Plugin API reference |
| [plugin-marketplaces](claude-code/plugin-marketplaces.md) | Marketplace creation and distribution |
| [discover-plugins](claude-code/discover-plugins.md) | Finding and installing plugins |
| [agent-teams](claude-code/agent-teams.md) | Multi-session orchestration |
| [keybindings](claude-code/keybindings.md) | Custom keyboard shortcuts |
| [output-styles](claude-code/output-styles.md) | Output formatting options |

Other topics (CI/CD, cloud providers, enterprise, troubleshooting): browse `docs/claude-code/` directly.
```

This is 38 lines. Preserves all daily-driver and extensibility entries with full descriptions. Removed sections are still accessible by browsing the directory -- just not routed from the CLAUDE.md.

Per RESEARCH.md Pitfall 3: "Trim by removing low-use categories while keeping daily-driver and extensibility sections intact."
  </action>
  <verify>
    <automated>wc -l docs/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l docs/CLAUDE.md` shows 50 or fewer lines
    - `grep 'Daily drivers' docs/CLAUDE.md` returns a match (section preserved)
    - `grep 'Extensibility' docs/CLAUDE.md` returns a match (section preserved)
    - `grep 'hooks' docs/CLAUDE.md` returns at least 1 match (daily driver entry preserved)
    - `grep 'plugins' docs/CLAUDE.md` returns at least 1 match (extensibility entry preserved)
    - `grep -c 'CI/CD' docs/CLAUDE.md` returns 0 (removed section)
    - `grep -c 'Cloud providers' docs/CLAUDE.md` returns 0 (removed section)
    - `grep 'browse.*directly' docs/CLAUDE.md` returns a match (fallback pointer exists)
  </acceptance_criteria>
  <done>docs/CLAUDE.md is under 50 lines. Daily drivers (12 entries) and Extensibility (7 entries) preserved with full descriptions. Removed sections noted with a one-line pointer to browse the directory.</done>
</task>

</tasks>

<verification>
Phase 1 Plan 02 verification:

1. Root coverage: `grep -c '@.*/' CLAUDE.md` shows 6 directory entries using @dir/ syntax
2. Thin routers exist: `ls bin/CLAUDE.md guestbook/CLAUDE.md notes/CLAUDE.md lab/CLAUDE.md` -- all 4 exist
3. Line budget: `wc -l $(find . -name CLAUDE.md -not -path './.claude/*' -not -path './.planning/*')` -- all under 50
4. docs trimmed: `wc -l docs/CLAUDE.md` shows under 50
5. Identity inline: `grep 'identity' CLAUDE.md` returns match in root
6. Tree preview syntax: `grep -c '\[.*\](.*CLAUDE.md)' CLAUDE.md` returns 0 in directories section (no markdown hyperlinks)
</verification>

<success_criteria>
- Root CLAUDE.md routes to all 6 content directories using @dir/ tree preview syntax (personal/, docs/, bin/, notes/, guestbook/, lab/)
- bin/, guestbook/, notes/, lab/ each have a CLAUDE.md with purpose, contents, and usage guidance
- docs/CLAUDE.md is under 50 lines with daily drivers and extensibility preserved
- No CLAUDE.md in the project (excluding .claude/ and .planning/) exceeds 50 lines
- Root CLAUDE.md includes identity context inline (who, what, core equation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-routing/01-02-SUMMARY.md`
</output>

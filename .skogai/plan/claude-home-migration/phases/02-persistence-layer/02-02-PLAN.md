---
phase: 02-persistence-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/context-journal.sh
  - bin/context-git.sh
  - bin/context-workspace.sh
  - bin/build-system-prompt.sh
  - bin/find-agent-root.sh
  - bin/context.sh
  - bin/CLAUDE.md
autonomous: true
requirements:
  - PER-01

must_haves:
  truths:
    - "All context scripts from scripts/context/ are accessible from bin/"
    - "bin/CLAUDE.md lists all scripts, not just healthcheck"
  artifacts:
    - path: "bin/context-journal.sh"
      provides: "Journal context builder"
    - path: "bin/context-git.sh"
      provides: "Git context builder"
    - path: "bin/context-workspace.sh"
      provides: "Workspace context builder"
    - path: "bin/build-system-prompt.sh"
      provides: "System prompt assembly"
    - path: "bin/find-agent-root.sh"
      provides: "Agent root detection"
    - path: "bin/context.sh"
      provides: "Context orchestrator"
    - path: "bin/CLAUDE.md"
      provides: "Updated bin/ router listing all scripts"
  key_links:
    - from: "personal/journal/CONVENTIONS.md"
      to: "bin/context-journal.sh"
      via: "Reading Journals section references bin/context-journal.sh"
      pattern: "bin/context-journal"
---

<objective>
Move context scripts from scripts/context/ to bin/ and update the bin/ router.

Purpose: Consolidate all executable scripts in bin/ (the canonical script location per Phase 1 routing). The scripts/context/ suite (built by Dot) handles journal reading, git context, workspace context, and system prompt assembly. Moving them to bin/ makes them first-class home directory tools alongside healthcheck. This directly supports PER-01 since the journal conventions doc references `bin/context-journal.sh` as the canonical journal reader.

Output: 6 scripts in bin/, updated bin/CLAUDE.md
</objective>

<execution_context>
@/home/skogix/claude/.claude/get-shit-done/workflows/execute-plan.md
@/home/skogix/claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-persistence-layer/02-CONTEXT.md
@.planning/phases/02-persistence-layer/02-RESEARCH.md

<interfaces>
<!-- Current bin/CLAUDE.md content -->
```markdown
# bin/ -- Executable scripts

Scripts and tools for home directory operations.

## contents

- `healthcheck` -- verifies environment sanity: home dir, gt cli, bd/beads, dolt server, git config, claude_home rig. Run: `./bin/healthcheck`

Add new scripts here. Each should be self-contained and runnable from project root.
```

<!-- Scripts to move (from scripts/context/) -->
- context-journal.sh -- Journal entries (supports flat + subdirectory formats)
- context-git.sh -- Git status + recent commits (with truncation)
- context-workspace.sh -- Workspace tree structure
- build-system-prompt.sh -- Reads gptme.toml and builds a full system prompt
- find-agent-root.sh -- Agent root detection
- context.sh -- Main orchestrator, calls all component scripts
- README.md -- Documentation (content to merge into bin/CLAUDE.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move scripts from scripts/context/ to bin/</name>
  <files>bin/context-journal.sh, bin/context-git.sh, bin/context-workspace.sh, bin/build-system-prompt.sh, bin/find-agent-root.sh, bin/context.sh</files>
  <read_first>
    - scripts/context/context-journal.sh (verify content before move)
    - scripts/context/context-git.sh (verify content before move)
    - scripts/context/context-workspace.sh (verify content before move)
    - scripts/context/build-system-prompt.sh (verify content before move)
    - scripts/context/find-agent-root.sh (verify content before move)
    - scripts/context/context.sh (verify content before move -- this is the orchestrator)
    - scripts/context/README.md (check for anything worth preserving in bin/CLAUDE.md)
  </read_first>
  <action>
1. Use `git mv` to move each script from scripts/context/ to bin/:
   - `git mv scripts/context/context-journal.sh bin/context-journal.sh`
   - `git mv scripts/context/context-git.sh bin/context-git.sh`
   - `git mv scripts/context/context-workspace.sh bin/context-workspace.sh`
   - `git mv scripts/context/build-system-prompt.sh bin/build-system-prompt.sh`
   - `git mv scripts/context/find-agent-root.sh bin/find-agent-root.sh`
   - `git mv scripts/context/context.sh bin/context.sh`

2. Check if any scripts have internal references to `scripts/context/` paths. If `context.sh` or `build-system-prompt.sh` source sibling scripts by relative path (e.g., `$(dirname "$0")/context-journal.sh`), those will still work since all scripts move together. If any script has a hardcoded `scripts/context/` path, update it to use `$(dirname "$0")/` instead.

3. Verify all moved scripts are executable: `chmod +x bin/context-*.sh bin/build-system-prompt.sh bin/find-agent-root.sh`

4. Remove `scripts/context/README.md` with `git rm scripts/context/README.md`. Then remove the empty `scripts/context/` directory with `rmdir scripts/context/`.

5. Do NOT create a `scripts/ -> bin/` symlink. The `scripts/` directory has 11 other subdirectories (bluesky/, discord/, github/, linear/, etc.) that are unrelated to this phase. The symlink would destroy access to those directories. Instead, leave `scripts/` as-is minus the now-empty `context/` subdirectory.
  </action>
  <verify>
    <automated>test -f bin/context-journal.sh && test -f bin/context-git.sh && test -f bin/context-workspace.sh && test -f bin/build-system-prompt.sh && test -f bin/find-agent-root.sh && test -f bin/context.sh && test -x bin/context-journal.sh && ! test -d scripts/context</automated>
  </verify>
  <acceptance_criteria>
    - bin/context-journal.sh exists and is executable
    - bin/context-git.sh exists and is executable
    - bin/context-workspace.sh exists and is executable
    - bin/build-system-prompt.sh exists and is executable
    - bin/find-agent-root.sh exists and is executable
    - bin/context.sh exists and is executable
    - scripts/context/ directory no longer exists (contents moved)
    - scripts/ directory still exists with its other 11 subdirectories intact
    - No scripts/ -> bin/ symlink created (would break other scripts/ subdirectories)
  </acceptance_criteria>
  <done>All 6 context scripts moved from scripts/context/ to bin/. scripts/context/ removed. scripts/ directory preserved with its other subdirectories.</done>
</task>

<task type="auto">
  <name>Task 2: Update bin/CLAUDE.md with new scripts</name>
  <files>bin/CLAUDE.md</files>
  <read_first>
    - bin/CLAUDE.md (current content -- only lists healthcheck)
    - scripts/context/README.md (content to merge -- script descriptions, design notes)
  </read_first>
  <action>
Rewrite `bin/CLAUDE.md` to list all scripts. New content:

```markdown
# bin/ -- Executable scripts

Scripts and tools for home directory operations.

## contents

- `healthcheck` -- verifies environment sanity: home dir, gt cli, bd/beads, dolt server, git config, claude_home rig. Run: `./bin/healthcheck`

### Context scripts

Generic context generation scripts (originally by Dot). Produce system prompt context for agents.

- `context.sh` -- main orchestrator, calls all component scripts. Usage: `./bin/context.sh [AGENT_DIR]`
- `context-journal.sh` -- journal entries (supports flat + subdirectory formats). Usage: `./bin/context-journal.sh [AGENT_DIR]`
- `context-git.sh` -- git status + recent commits (with truncation)
- `context-workspace.sh` -- workspace tree structure
- `build-system-prompt.sh` -- reads gptme.toml and builds a full system prompt
- `find-agent-root.sh` -- agent root detection

All context scripts accept an optional `AGENT_DIR` argument. Default: git repo root.
```

This replaces the old "Add new scripts here" placeholder with actual content. Keeps under 50 lines per CTX-03.
  </action>
  <verify>
    <automated>grep -q "context-journal" bin/CLAUDE.md && grep -q "healthcheck" bin/CLAUDE.md && grep -q "context-git" bin/CLAUDE.md && wc -l < bin/CLAUDE.md | xargs test 50 -ge</automated>
  </verify>
  <acceptance_criteria>
    - bin/CLAUDE.md contains "context-journal" (new script listed)
    - bin/CLAUDE.md contains "context-git" (new script listed)
    - bin/CLAUDE.md contains "context-workspace" (new script listed)
    - bin/CLAUDE.md contains "build-system-prompt" (new script listed)
    - bin/CLAUDE.md contains "find-agent-root" (new script listed)
    - bin/CLAUDE.md contains "healthcheck" (existing script preserved)
    - bin/CLAUDE.md is under 50 lines (CTX-03 compliance)
  </acceptance_criteria>
  <done>bin/CLAUDE.md lists all 7 scripts (healthcheck + 6 context scripts) with descriptions and usage.</done>
</task>

</tasks>

<verification>
- All 6 scripts exist in bin/ and are executable
- bin/CLAUDE.md lists all scripts
- scripts/context/ no longer exists
- scripts/ directory preserved with other subdirectories
</verification>

<success_criteria>
- Context scripts consolidated in bin/ -- single canonical location for all home directory scripts
- bin/CLAUDE.md accurately reflects all available scripts
- No broken references from the move
</success_criteria>

<output>
After completion, create `.planning/phases/02-persistence-layer/02-02-SUMMARY.md`
</output>
